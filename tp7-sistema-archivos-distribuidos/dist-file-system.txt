title Distributed file system
DNS->DNS:createFileTable()
Server->DNS:registerServer()
DNS->DNS:addServer()
Server->DNS:registerFile()
DNS->DNS:registerFileInTable()
DNS->DNS:addFile()
Client->DNS:requestFile(fileName, command)
DNS->DNS:searchFileInTable(fileName)
DNS->DNS:lockFileInTable(fileName)
DNS->Server:requestSendFile(fileName, requestIp, requestPort)
Server->Client:sendFile(fileName)
Client->DNS:sendModifiedFile(file, fileName)
DNS->Client:searchFile(fileName)
DNS->Server:sendModifiedFile(file, fileName)
Server->DNS:confirmFileModified(fileName)
DNS->DNS:unlockFileInTable(fileName)